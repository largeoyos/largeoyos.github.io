diff --git a/tools/random-number/index.html b/tools/random-number/index.html
--- a/tools/random-number/index.html
+++ b/tools/random-number/index.html
@@ -230,0 +231,288 @@
+    <style>
+        :root {
+            --lab-ink: #102033;
+            --lab-muted: #5d6b7b;
+            --lab-line: #c9d1dc;
+            --lab-soft: #eef2f6;
+            --lab-paper: #ffffff;
+            --lab-blue: #165dff;
+            --lab-blue-dark: #0c3fb4;
+            --lab-orange: #f26a2e;
+            --lab-red: #c9364f;
+            --lab-shadow: 6px 6px 0 #102033;
+        }
+        body {
+            min-width: 320px;
+            background:
+                linear-gradient(rgba(16, 32, 51, 0.045) 1px, transparent 1px),
+                linear-gradient(90deg, rgba(16, 32, 51, 0.045) 1px, transparent 1px),
+                #f4f6f8;
+            background-size: 24px 24px;
+            color: var(--lab-ink);
+        }
+        .global-nav { border-bottom: 3px solid var(--lab-blue); background: var(--lab-ink); }
+        .tool-hero {
+            padding: 54px 20px 72px;
+            border-bottom: 1px solid var(--lab-line);
+            background: rgba(255, 255, 255, 0.96);
+            text-align: left;
+        }
+        .tool-hero-inner { width: min(1100px, 100%); margin: 0 auto; }
+        .tool-kicker, .panel-index, .field-label, .result-label {
+            font-family: "Cascadia Code", "SFMono-Regular", Consolas, monospace;
+            letter-spacing: 0.12em;
+            text-transform: uppercase;
+        }
+        .tool-kicker {
+            display: inline-flex;
+            align-items: center;
+            gap: 10px;
+            margin-bottom: 14px;
+            color: var(--lab-blue);
+            font-size: 12px;
+            font-weight: 800;
+        }
+        .tool-kicker::before { width: 28px; height: 3px; background: var(--lab-blue); content: ""; }
+        .tool-hero h1 {
+            margin: 0;
+            color: var(--lab-ink);
+            font-family: "Iowan Old Style", "Noto Serif SC", "Songti SC", serif;
+            font-size: clamp(38px, 7vw, 72px);
+            line-height: 0.98;
+            letter-spacing: -0.04em;
+        }
+        .tool-hero p {
+            max-width: 680px;
+            margin: 18px 0 0;
+            color: var(--lab-muted);
+            font-size: 16px;
+            line-height: 1.8;
+        }
+        .hero-status { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 22px; }
+        .hero-status span {
+            padding: 6px 9px;
+            border: 1px solid var(--lab-line);
+            background: var(--lab-soft);
+            color: var(--lab-ink);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 11px;
+            font-weight: 700;
+        }
+        .rn-container {
+            width: min(1100px, calc(100% - 32px));
+            max-width: none;
+            margin: -38px auto 72px;
+            padding: 0;
+        }
+        .tab-bar {
+            position: relative;
+            z-index: 2;
+            width: fit-content;
+            border: 2px solid var(--lab-ink);
+            background: var(--lab-paper);
+            box-shadow: 4px 4px 0 var(--lab-ink);
+        }
+        .tab-btn {
+            min-width: 190px;
+            padding: 14px 20px;
+            border-right: 1px solid var(--lab-ink);
+            border-radius: 0 !important;
+            background: var(--lab-paper);
+            color: var(--lab-muted);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 13px;
+            letter-spacing: 0.03em;
+        }
+        .tab-btn:last-child { border-right: 0; }
+        .tab-btn:hover:not(.active) { background: var(--lab-soft); color: var(--lab-ink); }
+        .tab-btn.active { background: var(--lab-ink); color: #fff; }
+        .tab-btn:focus-visible { outline: 3px solid var(--lab-orange); outline-offset: 3px; }
+        .tab-panel {
+            margin-top: 18px;
+            padding: 0;
+            border: 2px solid var(--lab-ink);
+            border-radius: 0;
+            background: var(--lab-paper);
+            box-shadow: var(--lab-shadow);
+        }
+        .panel-head {
+            display: flex;
+            align-items: flex-start;
+            justify-content: space-between;
+            gap: 18px;
+            padding: 22px 24px;
+            border-bottom: 1px solid var(--lab-line);
+        }
+        .panel-index { color: var(--lab-blue); font-size: 11px; font-weight: 800; }
+        .panel-head h2 { margin: 5px 0 0; color: var(--lab-ink); font-size: 22px; }
+        .panel-head p {
+            max-width: 480px;
+            margin: 0;
+            color: var(--lab-muted);
+            font-size: 13px;
+            line-height: 1.7;
+            text-align: right;
+        }
+        .generator-workbench {
+            display: grid;
+            grid-template-columns: minmax(280px, 0.8fr) minmax(340px, 1.2fr);
+            min-height: 430px;
+        }
+        .control-deck {
+            padding: 26px 24px;
+            border-right: 1px solid var(--lab-line);
+            background: #f8fafc;
+        }
+        .control-deck h3, .result-deck h3 { margin: 0 0 18px; color: var(--lab-ink); font-size: 14px; }
+        .gen-controls, .bomb-controls {
+            display: grid;
+            grid-template-columns: repeat(2, minmax(0, 1fr));
+            gap: 14px;
+            margin: 0;
+        }
+        .field { display: flex; min-width: 0; flex-direction: column; gap: 7px; }
+        .field-label { color: var(--lab-muted); font-size: 10px; font-weight: 800; }
+        .gen-controls input, .bomb-controls input {
+            width: 100%;
+            min-height: 44px;
+            padding: 10px 12px;
+            border: 1px solid #95a1b1;
+            border-radius: 0;
+            background: #fff;
+            color: var(--lab-ink);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 15px;
+            text-align: left;
+        }
+        .gen-controls input:focus, .bomb-controls input:focus {
+            border-color: var(--lab-blue);
+            outline: 3px solid rgba(22, 93, 255, 0.14);
+        }
+        .input-note { margin: 14px 0 0; color: var(--lab-muted); font-size: 12px; line-height: 1.6; }
+        .gen-btns, .bomb-btns {
+            display: grid;
+            grid-template-columns: repeat(3, minmax(0, 1fr));
+            gap: 8px;
+            margin-top: 24px;
+        }
+        .gen-btns button, .bomb-btns button, .btn-restart, .utility-btn {
+            min-height: 44px;
+            padding: 10px 13px;
+            border: 1px solid var(--lab-ink);
+            border-radius: 0;
+            background: #fff;
+            color: var(--lab-ink);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 12px;
+            font-weight: 800;
+            cursor: pointer;
+            transition: transform 0.12s, box-shadow 0.12s, background 0.12s;
+        }
+        .gen-btns button:hover, .bomb-btns button:hover, .btn-restart:hover, .utility-btn:hover:not(:disabled) {
+            transform: translate(-2px, -2px);
+            box-shadow: 3px 3px 0 var(--lab-ink);
+        }
+        .gen-btns button:active, .bomb-btns button:active, .btn-restart:active, .utility-btn:active {
+            transform: translate(0, 0);
+            box-shadow: none;
+        }
+        .btn-gen-int, .btn-mode-int { background: var(--lab-blue) !important; color: #fff !important; }
+        .btn-gen-float { background: #d9f5ee !important; }
+        .btn-gen-complex, .btn-mode-complex { background: #ffe6da !important; }
+        .result-deck { display: flex; min-width: 0; flex-direction: column; padding: 26px 24px; }
+        .result-toolbar { display: flex; align-items: center; justify-content: space-between; gap: 12px; }
+        .result-label { color: var(--lab-muted); font-size: 10px; font-weight: 800; }
+        .utility-btn { min-height: 34px; padding: 7px 10px; }
+        .utility-btn:disabled { border-color: var(--lab-line); color: #99a3af; cursor: not-allowed; }
+        .gen-display {
+            flex: 1;
+            min-height: 250px;
+            margin: 14px 0 0;
+            padding: 28px;
+            border: 1px solid var(--lab-line);
+            border-radius: 0;
+            background: linear-gradient(rgba(22, 93, 255, 0.06) 1px, transparent 1px), #f9fbff;
+            background-size: 100% 28px;
+        }
+        .gen-display .gen-value {
+            overflow-wrap: anywhere;
+            color: var(--lab-blue);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: clamp(32px, 6vw, 58px);
+            line-height: 1.1;
+        }
+        .gen-grid { align-content: center; align-items: center; gap: 8px; }
+        .gen-grid span {
+            padding: 6px 8px;
+            border: 1px solid var(--lab-line);
+            background: #fff;
+            color: var(--lab-blue-dark);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 13px;
+        }
+        .feedback { min-height: 22px; margin-top: 12px; color: var(--lab-muted); font-size: 12px; line-height: 1.6; }
+        .feedback.error { color: var(--lab-red); font-weight: 700; }
+        .feedback.success { color: #087f5b; font-weight: 700; }
+        .bomb-workbench { display: grid; grid-template-columns: 290px minmax(0, 1fr); }
+        .bomb-sidebar { padding: 24px; border-right: 1px solid var(--lab-line); background: #f8fafc; }
+        .bomb-controls { grid-template-columns: 1fr; }
+        .bomb-btns { grid-template-columns: 1fr; margin-top: 18px; }
+        .bomb-console { margin-top: 20px; border: 1px solid var(--lab-ink); background: var(--lab-ink); color: #fff; }
+        .bomb-status {
+            min-height: 76px;
+            margin: 0;
+            padding: 14px;
+            color: #fff;
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 12px;
+            line-height: 1.65;
+            text-align: left;
+        }
+        .bomb-count {
+            margin: 0;
+            padding: 9px 14px;
+            border-top: 1px solid #3c4d61;
+            color: #9fb3ca;
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 11px;
+            text-align: left;
+        }
+        .bomb-restart { margin-top: 12px !important; margin-bottom: 0 !important; }
+        .btn-restart { width: 100%; background: var(--lab-orange); color: #fff; }
+        .bomb-board { min-width: 0; padding: 24px; }
+        .board-ruler {
+            display: flex;
+            justify-content: space-between;
+            margin-bottom: 12px;
+            color: var(--lab-muted);
+            font-family: "Cascadia Code", Consolas, monospace;
+            font-size: 10px;
+            letter-spacing: 0.08em;
+        }
+        .bomb-canvas-wrap { max-width: 620px; border: 1px solid var(--lab-line); background: #f9fbff; }
+        .bomb-canvas-wrap canvas { border: 0; border-radius: 0; background: transparent; cursor: crosshair; touch-action: manipulation; }
+        footer { color: var(--lab-muted); }
+        @media (max-width: 800px) {
+            .tool-hero { padding: 42px 18px 64px; }
+            .rn-container { width: min(100% - 24px, 680px); }
+            .tab-bar { width: 100%; }
+            .tab-btn { min-width: 0; padding-inline: 10px; }
+            .generator-workbench, .bomb-workbench { grid-template-columns: 1fr; }
+            .control-deck, .bomb-sidebar { border-right: 0; border-bottom: 1px solid var(--lab-line); }
+            .panel-head { flex-direction: column; }
+            .panel-head p { text-align: left; }
+        }
+        @media (max-width: 520px) {
+            .tool-hero h1 { font-size: 42px; }
+            .hero-status { display: none; }
+            .rn-container { margin-top: -28px; }
+            .tab-btn { font-size: 11px; }
+            .panel-head, .control-deck, .result-deck, .bomb-sidebar, .bomb-board { padding: 18px; }
+            .gen-controls, .gen-btns { grid-template-columns: 1fr; }
+            .gen-display { min-height: 200px; padding: 18px; }
+            .gen-grid span { font-size: 12px; }
+        }
+        @media (prefers-reduced-motion: reduce) {
+            *, *::before, *::after { scroll-behavior: auto !important; transition-duration: 0.01ms !important; animation-duration: 0.01ms !important; }
+        }
+    </style>
