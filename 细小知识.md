# 细小知识记录

这里用于记录平时遇到的一些细微但很有用的编程知识点。后续会按照日期不断追加内容。

## 2026-05-29

### C++ `string::rfind` 逆向查找

`rfind` 是 `std::string` 的成员函数，功能与 `find` 相反——从字符串的末尾开始向前查找子串或字符。

基本用法：
- `s.rfind("sub")` 返回子串最后一次出现的位置，未找到返回 `string::npos`
- `s.rfind("sub", pos)` 限制在位置 `pos` 之前搜索
- 常用于提取文件路径中的文件名（`path.rfind('/')`）、获取扩展名等
